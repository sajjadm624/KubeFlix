FROM python:3.10-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy app files
COPY . .

# Run the service (assuming main.py has your app)
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "5000"]
